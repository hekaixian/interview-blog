---
title: "DDD领域驱动设计"
date: 2026-09-02
categories:
  - 面试
description: "DDD领域驱动设计"
---

# DDD领域驱动设计
## 1、DDD是什么

DDD指通过统一语言、业务抽象、领域划分和领域建模等一些列手段来控制软件复杂度的方法论，

主要是用来指导如何解耦业务系统，划分业务模块，定义业务领域模型及其交互。

## 2、充血模型和贫血模型

实体的充血模型强调将业务逻辑和行为封装在实体中，

将实体打造成具有丰富行为和状态的对象，它不仅仅是简单的数据容器，还包含了业务逻辑和处理规则

## 3、实体

拥有唯一标识的对象，它具有可变的状态和生命周期，实体 = 唯一身份标识 \+ 可变性【状态 \+ 行为】，

实体通常被表示为DO（领域对象）的形式

## 4、值对象

也称为数值对象或数据传输对象，是用于封装一组相关属性的对象，

它们没有唯一的标识，通常用于表示较简单的概念和属性组合

## 5、聚合

把一些关联性极强、生命周期一致的实体、值对象放到一个聚合里。聚合是领域对象的显式分组。

## 6、聚合根

也称为根实体，是一种更大范围的封装，把一组有相同生命周期，

在业务上不可分隔的实体和值对象聚合在一起，通过根实体的唯一标识对外提供能力
